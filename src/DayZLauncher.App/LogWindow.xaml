<Window x:Class="DayZLauncher.App.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DayZ Manager - Логи" Icon="assets/app.ico" Height="640" Width="960" MinHeight="360" MinWidth="560"
        ResizeMode="CanResize" WindowStyle="None"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        Background="{DynamicResource BgWindowBrush}" Foreground="{DynamicResource FgTextBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
    <DockPanel>
        <!-- Same custom title bar as MainWindow - native chrome's white/light caption in light theme
             didn't match this app's own (darker) light-theme palette, so this window gets the same
             fully custom-drawn title bar instead of relying on DWM's immersive-dark-mode toggle. -->
        <Grid DockPanel.Dock="Top" Height="36" Background="{DynamicResource BgPanelBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                <Image Source="assets/app.ico" Width="16" Height="16" Margin="0,0,8,0"/>
                <TextBlock Text="DayZ Manager - Логи" VerticalAlignment="Center" FontSize="12"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="&#x2014;" Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" AutomationProperties.Name="Свернуть" ToolTip="Свернуть"/>
                <Button x:Name="MaximizeButton" Content="&#x2750;" Style="{StaticResource TitleBarButtonStyle}" Click="MaximizeButton_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" AutomationProperties.Name="Развернуть" ToolTip="Развернуть"/>
                <Button Content="&#x2715;" Style="{StaticResource CloseTitleBarButtonStyle}" Click="CloseButton_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" AutomationProperties.Name="Закрыть" ToolTip="Закрыть"/>
            </StackPanel>
        </Grid>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="10,10,10,8">
            <TextBlock Text="Источник:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <RadioButton Content="Клиент" GroupName="logsource" IsChecked="{Binding IsClientSource}" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <RadioButton Content="Сервер" GroupName="logsource" IsChecked="{Binding IsServerSource}" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <TextBlock Text="Тип:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Width="90" Margin="0,0,20,0" ItemsSource="{Binding LogTypeOptions}" SelectedItem="{Binding SelectedLogType}"/>
            <Button Content="Обновить список" Command="{Binding RefreshFilesCommand}"/>
            <Button Content="Открыть папку" Command="{Binding OpenFolderCommand}"/>
            <Button Content="Удалить логи" Command="{Binding DeleteLogsCommand}"/>
            <CheckBox Content="Автопрокрутка" IsChecked="{Binding AutoScroll}" VerticalAlignment="Center" Margin="12,3,0,3"/>
            <TextBlock Text="Фильтр:" VerticalAlignment="Center" Margin="16,0,6,0"/>
            <TextBox Width="160" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <ListBox DockPanel.Dock="Left" Width="280" Margin="10,0,8,10"
                 ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedFile}" DisplayMemberPath="FileName"/>

        <TextBox x:Name="LogTextBox" Text="{Binding Content, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True"
                 TextWrapping="NoWrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" Background="{DynamicResource BgControlBrush}" Foreground="{DynamicResource FgTextBrush}"
                 Margin="0,0,10,10"/>
    </DockPanel>
    </Border>
</Window>
